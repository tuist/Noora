{
  "name": "@tuist/noora-docs",
  "private": true,
  "devDependencies": {
    "vitepress": "^1.5.0",
    "wrangler": "^3.50.0",
    "cookie": ">=0.7.0",
    "esbuild": ">=0.25.0"
  },
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "preview": "vitepress preview .",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name tuist-noora --branch main"
  }
}
