{
  "name": "@tuist/swiftterminal-docs",
  "private": true,
  "devDependencies": {
    "vitepress": "^1.1.0",
    "wrangler": "^3.50.0"
  },
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "preview": "vitepress preview .",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name tuist-swiftterminal --branch main"
  }
}